name: Hassfest Validation

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]
  release:
    types: [published, created, edited]
  schedule:
    - cron: "0 0 * * *"

jobs:
# ... a többi rész marad változatlan ...
